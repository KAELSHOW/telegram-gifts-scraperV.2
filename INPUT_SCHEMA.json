{
  "title": "Telegram Gifts Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "telegramUrl": {
      "title": "Telegram Gifts URL",
      "type": "string",
      "description": "Ссылка на страницу с подарками Telegram (по умолчанию https://telegram.org/gifts)",
      "default": "https://telegram.org/gifts",
      "editor": "textfield"
    },
    "maxItems": {
      "title": "Максимум подарков",
      "type": "integer",
      "description": "Сколько подарков собрать за один запуск",
      "default": 50,
      "minimum": 1,
      "maximum": 500,
      "editor": "number"
    },
    "includeRare": {
      "title": "Включать редкие подарки?",
      "type": "boolean",
      "description": "Если выключить, редкие подарки не будут собираться",
      "default": true,
      "editor": "checkbox"
    },
    "saveImages": {
      "title": "Сохранять изображения?",
      "type": "boolean",
      "description": "Сохранять ссылки на изображения подарков в Dataset",
      "default": false,
      "editor": "checkbox"
    },
    "outputFormat": {
      "title": "Формат вывода",
      "type": "string",
      "description": "В каком формате сохранить результат (json, csv)",
      "default": "json",
      "enum": ["json", "csv"],
      "editor": "select"
    }
  },
  "required": ["telegramUrl"]
}
